<p>
  This activity will start at
  <%= activity.start_at.to_s(:long) %> and will end at <%= activity.ends_at.to_s(:long) %>.
</p>
<p>Location: <strong><%= activity.location %></strong></p>
<p><%= activity.description %></p>
<p>
  The status of the activity is
  <% if activity.definite %>
    <span class="label label-success">definite</span>
  <% else %>
    <span class="label label-warning">not definite</span>
  <% end %>
</p>

<% unless activity.definite %>
  <p>
    <%= render 'votes/vote_button', activity: activity %>
  </p>
<% end %>

<%= link_to 'Edit', edit_activity_path(activity), class: 'btn btn-default' if can?(:edit, activity) %>
<%= link_to 'Delete', activity_path(activity), method: :delete, class: 'btn btn-danger' if can?(:delete, activity) %>
